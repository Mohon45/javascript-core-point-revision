function performOperation(secondInteger, secondDecimal, secondString) {
  const firstInteger = 4;
  const firstDecimal = 4.0;
  const firstString = "HackerRank ";

  const intergerNumber = firstInteger + secondInteger;
  console.log(intergerNumber);

  const decimalNumber = firstDecimal + secondDecimal;
  console.log(decimalNumber);

  const string = firstString + secondString;
  console.log(string);
}

performOperation(12, 4.32, "is the best place to learn and practice coding!");
